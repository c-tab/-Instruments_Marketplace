<%= simple_form_for [@instrument, @booking], data: { controller: 'flatpickr' } do |f| %>
  <%= f.input :booking_startdate, as: :string, input_html: { data: { flatpickr_target: "startTime" } } %>
  <%= f.input :booking_enddate, as: :string, input_html: { data: { flatpickr_target: "endTime" } } %>
  <%= f.submit 'Schedule your booking' %>
<% end %>
